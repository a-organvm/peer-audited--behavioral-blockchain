# Plan: Live Project Board Apply

Date: 2026-03-09
Repo: `peer-audited--behavioral-blockchain`
Target: GitHub project `organvm-iii-ergon` project `#2`

## Goal

Apply the safest high-value project-board changes directly to the live GitHub project without scrambling existing data.

## Steps

1. Create the new project fields needed for founder/partner operations.
2. Update project metadata (`readme`, description) to reflect the new model.
3. Populate the new fields on critical Beta/Gamma/Omega partner issues and the phase epics.
4. Rename fields only where safe; defer destructive option rewrites unless proven safe.
5. If view creation is not available through the GitHub API, document that gap and leave the saved-view layout for a manual/browser pass.

## Constraints

- Do not mass-edit 500+ items using weak heuristics.
- Do not replace existing single-select options unless safe behavior is confirmed.
- Prioritize Beta blockers and partner-owned issues first.
