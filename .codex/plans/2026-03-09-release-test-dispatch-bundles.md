# Release Test Dispatch Bundles Plan

Date: 2026-03-09

Objective:

- convert the Phase 1 beta release bar into owner-ready test bundles
- keep the split small enough to dispatch immediately

Result:

- `Bundle A — Runtime Truth` = `320`
- `Bundle B — Surface Truth` = `45`
- `Bundle C — Financial + Release Truth` = `49` required / `64` preferred

Totals:

- required = `414`
- preferred = `429`
